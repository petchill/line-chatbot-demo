let express,db;_ce4‍.x([["router",()=>router]]);_ce4‍.w("express",[["default",["express"],function(v){express=v}]]);_ce4‍.w("../firebase",[["db",["db"],function(v){db=v}]]);


       const router = express.Router()

const getData = async () => {
  const ref = db.ref('rooms')
  // ref.once("value", function(snapshot) {
  //   console.log(snapshot.val());
  // });
  const snapshots = await ref.once('value')
  return snapshots
}
router.get('/', async (req, res) => {
  const snapshots = await getData()
  _ce4‍.g.console.log('value => ', await snapshots.val());
  res.json({
    data: snapshots
  })
})
  